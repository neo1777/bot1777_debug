# .env.master.example (Template Unificato)

Questo file contiene tutte le variabili d'ambiente utilizzate nel progetto BotBinance1777.
Copia le parti necessarie nei rispettivi file `.env` locali.

## 🔑 Binance API (Backend)
# Inserite in: neotradingbotback1777/.env
BINANCE_API_KEY=your_binance_api_key
BINANCE_API_SECRET=your_binance_secret_key
TEST_BINANCE_API_KEY=your_binance_testnet_api_key
TEST_BINANCE_API_SECRET=your_binance_testnet_secret_key

## 🌐 gRPC & TLS (Backend & Ops)
# Inserite in: neotradingbotback1777/.env
GRPC_HOST=127.0.0.1
GRPC_PORT=50051
ALLOW_INSECURE_GRPC=false
STRICT_BOOT=false

# Autenticazione API Key (opzionale ma raccomandato)
# Se configurata, ogni client gRPC deve inviare header x-api-key
GRPC_API_KEY=

# Percorsi certificati (Volume Docker /certs)
CERT_PATH=/certs/server.crt
KEY_PATH=/certs/server.key

## ⚡ Performance & Trading Tuning (Backend)
BINANCE_RECV_WINDOW_MS=5000
BINANCE_MAX_WEIGHT_1M=1200
MAX_TRADE_AMOUNT_CAP=1000000
ISOLATE_SENDPORT_TIMEOUT_S=30

## 🧪 Testing & Safety
# Forza la modalità test nel backend indipendentemente dalla UI
DRY_RUN=false

## 🚀 Deployment (Ops)
# Inserite in: scripts/.env.deploy
VPS_IP=1.2.3.4
VPS_USER=root
# Autenticazione SSH (scegliere uno dei due):
VPS_SSH_KEY=~/.ssh/id_rsa
# VPS_PASS=your_secure_password  # deprecato, usare VPS_SSH_KEY
DOCKER_IMAGE_NAME=neotradingbot-backend
DOCKER_TAG=latest